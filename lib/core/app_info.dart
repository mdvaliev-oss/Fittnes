/// Human-readable app version shown in the UI. Keep in sync with the
/// `version:` field in pubspec.yaml — bump both when releasing a new build so
/// users can confirm at a glance which APK they're running.
const String kAppVersion = '0.3.0';
